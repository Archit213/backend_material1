# Handling and working with the forms
```
- handle backend process of forms and making sure the data coming from any frontend lib, fw, templating engines
- npm i cookie-parser -> install cookie parser package
```

## Session cookie
```
We can keep any data on the frontend of the browser and when we request from the backend the data saved gets sent tobackend.
blob -> mixture of data i.e sent to the server (it is not directly readable) server also converts it into readable format.
```